name: Download WPS rankings 2009-present

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Automatic IPC ranking extraction is disabled."
          echo "IPC Services returns HTTP 403 to automated clients and the embedded form is unavailable in GitHub Actions."
          echo "Use verified archived ranking files from federations and official historical documents instead."
